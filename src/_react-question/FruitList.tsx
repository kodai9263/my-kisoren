// 与えられた配列 const fruits = ['りんご', 'みかん', 'バナナ'] を利用して、それぞれをリスト（<ul>）として画面に表示するコンポーネントを作成してください。

export default function FruitList() {
  const fruits = ['りんご', 'みかん', 'バナナ'];


};






































/*return (
  <div>
    <ul>
      {fruits.map((fruit, index) => (
        <li key={index}>{fruit}</li>
      ))}
    </ul>
  </div>
)*/
