// propsを使用して、親コンポーネントから子コンポーネントへ文字列を渡し、それを子コンポーネントで表示させてください。

// 親コンポーネント
export default function ParentComponent() {
  return (
    <div>
      {/* ここに子コンポーネントを設置し、propsでメッセージを渡してください */}
    </div>
  );
}

// propsの型を定義


// 子コンポーネント (ChildComponent)
// ここに、propsを受け取り表示する、子コンポーネント(ChildComponent)を作成してください
const ChildComponent = 






















/*export default function ParentComponent() {
  return (
    <div>
      <ChildComponent message="親からのメッセージです" />
    </div>
  );
}

interface ChildComponentProps {
  message: string;
}

const ChildComponent = ({ message }): ChildComponentProps => {
  return <p>{message}</p>
}*/