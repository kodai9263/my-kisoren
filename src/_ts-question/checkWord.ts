// 指定された文字列の中に、特定の単語(例:「楽しい」)が含まれているかを判定する関数を作って、結果をconsole.logで出しちゃってね

const text = '今日は楽しい';









































/* ① 判定対象の文字列を定義
const text = "今日はめっちゃ楽しい☆";

② 特定の単語が含まれているかどうか判定する関数をここに定義
const checkWord = ((text: string) => text.includes("楽しい"));

③ その関数を使って判定結果をconsole.logで表示
console.log(checkWord(text));*/